// filepath: app/simulation/projects/layout.tsx
// AppNav is already injected by the parent app/simulation/layout.tsx
export default function ProjectsLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}
